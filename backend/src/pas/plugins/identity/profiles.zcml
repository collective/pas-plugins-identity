<configure
    xmlns="http://namespaces.zope.org/zope"
    xmlns:genericsetup="http://namespaces.zope.org/genericsetup"
    i18n_domain="pas.plugins.identity"
    >

  <genericsetup:registerProfile
      name="default"
      title="pas.plugins.identity: Install"
      description="Multi-provider external authentication for Plone."
      provides="Products.GenericSetup.interfaces.EXTENSION"
      directory="profiles/default"
      post_handler=".setuphandlers.post_install"
      />

  <genericsetup:registerProfile
      name="uninstall"
      title="pas.plugins.identity: Uninstall"
      description="Uninstall pas.plugins.identity setup."
      provides="Products.GenericSetup.interfaces.EXTENSION"
      directory="profiles/uninstall"
      post_handler=".setuphandlers.post_uninstall"
      />

  <!-- Hide the uninstall profiles of all three layers. One utility is
       enough: the add-ons control panel collects every INonInstallable it
       can find and merges what they hide, so the name this is registered
       under has nothing to do with which package a hidden profile is in. -->
  <utility
      factory=".setuphandlers.HiddenProfiles"
      name="pas.plugins.identity"
      />

  <include package=".upgrades" />

</configure>
