"""What this package adds to plone.restapi's view of a principal.

One adapter so far, and it is registered on this package's own browser layer:
a site that has not installed the add-on gets stock ``@users`` back, unchanged.
"""
