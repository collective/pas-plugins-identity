"""Mechanics with no policy in them.

The signing ring the server mints keys from, and the bridge that lets a
browser already authenticated to Volto reach the authorization endpoint.
Neither decides anything about a grant; both are asked by the code that does.
"""
