<configure
    xmlns="http://namespaces.zope.org/zope"
    xmlns:genericsetup="http://namespaces.zope.org/genericsetup"
    i18n_domain="pas.plugins.identity"
    >

  <!-- GenericSetup builds a profile id from the package the directive
       appears in, so these install as ``pas.plugins.identity.content:*``
       rather than as extra profiles of the distribution. That is deliberate
       (Érico, 2026-08-27): the layer is optional, it is installed and
       uninstalled on its own, and the add-ons control panel lists it as its
       own entry with its own install and uninstall profile — which is what
       it is. The panel shows no version for it, because the id is a package
       rather than a distribution and there is no metadata to read. -->

  <genericsetup:registerProfile
      name="default"
      title="pas.plugins.identity: Users and groups as content"
      description="Content-backed users and groups, with a dedicated catalog."
      provides="Products.GenericSetup.interfaces.EXTENSION"
      directory="profiles/default"
      post_handler=".setuphandlers.post_install"
      />

  <!-- A profile rather than a site-wide import step. An import step is
       registered once for the whole site and runs during *every* add-on
       installation, so keeping it from clearing an innocent catalog needed
       a marker file in the profile directory and a guard reading it. A
       profile runs when somebody applies it and never otherwise, which is
       the behaviour that guard was reconstructing. -->
  <genericsetup:registerProfile
      name="rebuild-catalog"
      title="pas.plugins.identity: Rebuild the user catalog"
      description="Clear the dedicated user catalog and index every user profile in the site again. Re-runnable, and safe to run at any time."
      provides="Products.GenericSetup.interfaces.EXTENSION"
      directory="profiles/rebuild-catalog"
      post_handler=".setuphandlers.rebuild_catalog"
      />

  <genericsetup:registerProfile
      name="uninstall"
      title="pas.plugins.identity: Uninstall users and groups as content"
      description="Remove the user catalog and the content type registrations."
      provides="Products.GenericSetup.interfaces.EXTENSION"
      directory="profiles/uninstall"
      post_handler=".setuphandlers.post_uninstall"
      />

</configure>
