<?xml version="1.0" encoding="utf-8"?>
<object xmlns:i18n="http://xml.zope.org/namespaces/i18n"
        meta_type="Dexterity FTI"
        name="UserGroup"
        i18n:domain="pas.plugins.identity"
>
  <property name="title"
            i18n:translate=""
  >User Group</property>
  <property name="description"
            i18n:translate=""
  >A group, backed by content. Membership is a field on each Profile, not a list here.</property>
  <!-- A group holds groups and nothing else. Filing one inside another is
       how a team hierarchy is drawn, and the containing group becomes an
       outer group of the one inside it. See core.utils.nesting. -->
  <property name="allowed_content_types">
    <element value="UserGroup" />
  </property>
  <property name="filter_content_types">True</property>
  <!-- Same lock as the UserProfile type: see that FTI. Structurally addable
       anywhere, actually addable only where the add permission is granted.
       A group inside the configured container acquires that grant from it,
       which is what lets a group be added inside a group. -->
  <property name="global_allow">True</property>
  <property name="icon_expr">string:users</property>
  <property name="klass">pas.plugins.identity.core.contents.group.UserGroup</property>
  <property name="schema">pas.plugins.identity.core.contents.group.IUserGroupSchema</property>
  <property name="add_permission">pas.plugins.identity.usergroup.add</property>
  <property name="link_target" />
  <property name="model_source" />
  <property name="model_file" />
  <property name="behaviors">
    <element value="pas.plugins.identity.group_membership" />
    <element value="pas.plugins.identity.global_roles" />
    <element value="plone.shortname" />
    <element value="plone.versioning" />
  </property>

  <alias from="(Default)"
         to="(dynamic view)"
  />
  <alias from="edit"
         to="@@edit"
  />
  <alias from="view"
         to="(selected layout)"
  />

  <action action_id="view"
          category="object"
          condition_expr=""
          title="View"
          url_expr="string:${object_url}"
          visible="True"
          i18n:attributes="title"
  >
    <permission value="View" />
  </action>
  <action action_id="edit"
          category="object"
          condition_expr=""
          title="Edit"
          url_expr="string:${object_url}/edit"
          visible="True"
          i18n:attributes="title"
  >
    <permission value="Modify portal content" />
  </action>
</object>
