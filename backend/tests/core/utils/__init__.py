"""The helpers under ``core.utils``: normalising, sanitizing, resolving.

Unit tests, and they are unit tests because the code under them has no policy
in it and touches nothing. Anything here that needs a portal is in the wrong
directory.
"""
