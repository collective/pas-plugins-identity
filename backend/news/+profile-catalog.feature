Added the `[profile]` extra's content layer: an `IdentityProfile` Dexterity type, a three-state workflow (`incomplete` / `complete` / `deactivated`), and a dedicated `portal_identity_catalog` carrying the whole PAS property sheet as metadata, so the plugins in the next gate can answer from brains without waking a single object. Installed by its own `pas.plugins.identity:profile` GenericSetup profile; uninstalling removes the catalog, the type and the workflow and leaves every Profile and its data untouched, because uninstalling an add-on is a configuration change and not an instruction to delete accounts. Where Profiles are stored is configuration, not a constant — the container's parent, id, title and type are four registry records, and the catalog indexes a Profile wherever it actually is, so reorganising content is not a deauthentication. Drift between the catalog and the site is reported by a read-only consistency check and repaired by a re-runnable `rebuild-profile-catalog` import step, kept apart so the check can be scheduled without a side effect. @ericof
