Added provider metadata resolution: published endpoint constants for providers that do not move (GitHub), and OIDC discovery for the rest, cached per issuer so a login costs one round trip rather than three. Discovery refuses a document that declares a different issuer than the one asked (RFC 8414), and an HTTPS issuer cannot downgrade the fetch of its own key set to plain HTTP. @ericof
