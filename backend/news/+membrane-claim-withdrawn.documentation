Withdrew an unverified claim about `Products.membrane`. A module docstring stated that its user-properties plugin wakes content objects, which was written from memory and never measured; membrane is not a dependency here, so nothing in the test suite could have caught it. What the code and the documentation now say is only what is actually tested: that this package serves properties, enumeration and group membership without waking a content object, and proves it on every CI run. @ericof
