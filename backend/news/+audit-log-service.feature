Added `GET @audit-log`. The default scope is the caller's own authentication events; reading another user's, or the site-wide log including the refusals that could not be attributed to anybody, needs the `Manage portal` permission. The default is deliberately the narrow one — a log that shows the whole site to whoever asks is a list of who has accounts and when they signed in. @ericof
