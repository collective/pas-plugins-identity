The `[server]` layer now implements the authorization code flow. `@@oauth-authorize` issues a code to a registered client and `@@oauth-token` exchanges it for an access token, both as browser views rather than REST services because a relying party sends a browser and posts a form, not JSON. PKCE is mandatory for public clients and only S256 is accepted; `plain` puts the verifier in the authorization request, which is the exact thing PKCE protects. Codes are single-use, short-lived, bound to the client and redirect URI they were issued for, and burned even when the redemption fails, so one intercepted code does not become unlimited guesses at the verifier. Both endpoints are bound to the server browser layer, so a site that never applied the profile does not publish them at all. @ericof
