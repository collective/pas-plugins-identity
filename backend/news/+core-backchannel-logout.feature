Added OpenID Connect back-channel logout (D6, C4 parity). A provider can now tell this site directly — server to server, with no browser involved — that somebody's session there has ended, which is exactly why it still works after the user has closed the tab. Register `@@backchannel-logout` as the client's back-channel logout URI; one endpoint serves every configured provider, since the logout token names its issuer and that is how the verifying key is chosen.

The `sub` in a logout token is the *provider's* subject, not a Plone userid — this package mints uuid4 userids (D10) that no provider has ever seen — so the identity store is what turns one into the other. A logout for an identity this site has never seen answers `200` rather than an error: there is nothing to end, and answering differently would tell an unauthenticated caller which of a provider's subjects have accounts here. Validation follows the specification: signature, issuer, audience, `iat` and `jti` are checked, the token must declare the back-channel logout event, must carry a `sub` or a `sid`, and must **not** carry a `nonce` — a nonce means somebody is trying to pass an `id_token` off as a logout instruction. A `jti` already acted on is refused as a replay, and is recorded before any work is done so a token cannot be acted on twice even if the first attempt failed halfway.

Ending the session needs `plone.session`'s **`per_user_keyring`**, which is off by default: without it every ticket in the site is signed from one ring, so ending one person's would end everybody's. This package refuses to do that, logs an error naming the switch, and reports `sessions_ended: False` on the event rather than silently doing nothing. Enable it at `acl_users/session` → *Manage secrets*.

A logout also revokes the refresh tokens the `[server]` layer issued for that user, across every client, because the logout was about the person rather than one application. That crosses a layer boundary core is forbidden to cross, so it goes through a new `SessionsRevoked` event: core fires it, and the `[server]` layer subscribes. Access tokens are **not** revoked — they are self-encoded with no denylist (D3) and live out their lifetime, at most the configured access-token TTL. That is the cost of the self-encoded design, and it is documented rather than hidden. @ericof
