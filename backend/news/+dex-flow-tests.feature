Added the end-to-end flow tests: Dex runs in Docker via pytest-docker, and the suite drives a browser-less authorization-code flow against it — fetch providers, follow the authorize redirect, log in at Dex's own form, hand the code back — ending with a `jwt_auth` token that fetches `@site` as the authenticated user. The refusals are exercised against codes a real provider actually issued. @ericof
