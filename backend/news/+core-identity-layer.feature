Added the core identity layer: an identity store mapping `(provider, subject)` pairs to a permanent canonical userid, a driver framework with GitHub, Google, generic OIDC and email drivers, the five-event contract, a PAS plugin providing extraction, authentication, credentials reset and opt-in challenge, registry-backed provider configuration with write-only secrets, and authlib-backed authorization-code flows carrying state, PKCE and nonce. @ericof
