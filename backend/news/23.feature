Audit records now go to every destination a site names, rather than to a single replaceable utility. Sinks are named utilities listed in order by `pas.plugins.identity.audit_sinks`, and one that fails or no longer resolves is logged and stepped over rather than allowed to fail the sign-in it was auditing. A new `IAuditSource` splits reading from writing, so a write-only destination is possible and a site with nothing readable is told so rather than shown an empty log. @ericof
