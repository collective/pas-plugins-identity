Added the linking UI and the provider control panel. `/identities` lets a signed-in user see their sign-in methods, add another, and remove one — with the remove button disabled and explained when it is their last way in, rather than failing only once pressed. `/controlpanel/identity-providers` renders each provider's form from its driver's published schema, so adding a driver on the backend adds its form here with no frontend change; it also runs the per-provider connection check and reports what it found. A stored secret is rendered as the mask the backend sent and saved straight back unless edited, which is what preserves it. @ericof
