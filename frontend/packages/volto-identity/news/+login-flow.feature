Added the login flow. The `/login` route renders a button per configured provider and, when magic-link login is enabled, an "email me a link" form; `/login-identity` is the route providers redirect back to, which hands the code or the emailed token to the backend and receives a `jwt_auth` token. Post-login redirect targets are restricted to site-relative paths, so a target that never reaches the backend cannot become an open redirect either. The magic-link form says the same thing whether or not the address is known, matching the backend. @ericof
