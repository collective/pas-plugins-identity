Added `DECISIONS.md` and `HUMAN-ACTIONS.md`, which Gate 0 called for and which had never been written. The first records the decisions taken while building — the ones a later reader might otherwise reopen or "clean up" — with the reason each was taken, including the withdrawal of the unverified claim about `Products.membrane`. The second lists what only a person can do and what each thing is blocking: deciding whether there is a remote at all (which is what keeps every gate short of GREEN), replacing the placeholder security contact before any public release, and the deployment decisions that carry a security or privacy consequence. Both also carry the Gate 7 migration spike: what reading the `pas.plugins.authomatic` and `pas.plugins.oidc` sources actually established, rather than what was remembered about them. @ericof
