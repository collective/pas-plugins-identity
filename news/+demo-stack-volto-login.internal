The federation demo's identity provider now serves Volto's login page rather than Plone's Classic one. Only the cookie-auth challenge path is routed to the backend; `/login` is Volto's, which is what a Volto-first identity provider should look like. What made that possible is the `[server]` layer reading Volto's session at the authorization endpoint, and the login page offering a password again. @ericof
