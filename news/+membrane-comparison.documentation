Grounded the README's "Why not Products.membrane" section, which previously said the comparison had not been measured. Membrane's property plugin does wake content objects: `getPropertiesForUser` collects providers through `findMembraneUserAspect`, which adapts `brain._unrestrictedGetObject()`, and nothing caches that path. Its user enumeration is unaffected and stays on the brains, so the claim is now scoped to properties and attributed to the version read. @ericof
