Dropped the "Gate S3" codename from the prose. It named a step in the plan this package was built to, which nobody reading the repository afterwards has: the two stacks are the *hermetic federation stack* in `backend/tests/federation` and the *manual federation stack* in `docker-compose.demo.yml`, and saying so is the whole change. No behaviour, no file names, no configuration. @ericof
