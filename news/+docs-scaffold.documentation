Added the documentation site: Sphinx with MyST, built in CI with warnings as errors and published from `docs/`. It covers installing and configuring providers, the audit log and its privacy defaults, the `[profile]` extra, writing a driver, and — the deliverable Gate 4 was waiting on — the event contract this package treats as its public API. The README gained the feature summary, the relationship to `pas.plugins.oidc` and `pas.plugins.authomatic`, and the "why not Products.membrane" section; `SECURITY.md` records how to report a vulnerability and what this package guarantees. @ericof
