Documented both migrations, including what each one refuses to do and why, and how to read a migration report. Also recorded why the authomatic-compatible callback route was skipped: migrating sites update the redirect URI at each provider once, which is a better trade than a second permanent entry point into the login flow carrying its own open-redirect and session-binding surface. @ericof
