Documented OpenID Connect discovery in the claims contract: what a relying party gets by pointing a client at the issuer URL, why every advertised endpoint is built from the configured issuer rather than the portal URL, and the difference between the `id_token` (a signed statement the relying party reads itself) and the userinfo endpoint (the same claims, for clients that prefer to ask).
