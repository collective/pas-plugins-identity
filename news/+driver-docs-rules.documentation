Documented three rules a driver author now has to follow: every config field declares an `order`, and no two share one; `default_scope` is a tuple of permissions rather than the space-delimited string that goes on the wire; and `default_propertymap` seeds a new provider's attribute mapping, written against the normalized claim names and naming only member fields a stock site actually has. The worked GitLab example carries all three. @ericof
