Documented the `groups` claim in the claims reference, and corrected the scope table there: it had listed `profile` as releasing three claims when the server had been releasing five for some time. The section on unregistered claims now names both of them and states the trade `groups` makes by riding on a display scope. @ericof
