StylesPath = styles

MinAlertLevel = suggestion

Vocab = Base,Plone

Packages = Microsoft

[*]
BasedOnStyles = Vale, Microsoft
# MyST cross-reference targets, e.g. (how-to-provider-keycloak)=, are markup
# identifiers rather than prose: they are never rendered and never read.
TokenIgnores = (\([a-z0-9][a-z0-9-]*\)=)
Microsoft.Contractions = suggestion
Microsoft.Units = suggestion
